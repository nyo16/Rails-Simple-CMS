<%= render "admin/partials/form_validations" %>
	<%= form_for([:admin, @gallery], :html => { :class => "form_validations" }) do |f| %>
	<div class="module_content">
	  <% if @gallery.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@gallery.errors.count, "error") %> prohibited this page from being saved:</h2>
	      <ul>
	      <% @gallery.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  	<%= f.label :name %>
	  	<%= f.text_field :name, :class =>"validate[required]" %>

	</div>
	<div style="clear: both"></div>
  	<br />
	  <footer>
	  	<div class="submit_link">
	    	<%= f.submit nil, :class => "alt_btn" %>
	  	</div>
	   </footer>
	<% end %>

